rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // storage/ papkasidagi barcha fayllarni ochiq o'qishga ruxsat
    match /storage/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
